/* <auto-generated> PungentFunk experimental Unity Editor skin bridge. */
/* This file is generated from the Appearance Lab. Disable the bridge to restore an empty extension sheet. */
/* It uses Unity editor USS extension selectors only; it does not patch editor resources or wrap IMGUI callbacks. */
/* Raw preset readability should be judged with the bridge disabled. The bridge is a broader editor-theme preview and can shift apparent contrast inside the Appearance Lab itself. */
/* Some Unity internals, especially script/asset preview implementations that stay in IMGUI or hidden native containers, may not expose stable USS hooks in every version. */
/* Hierarchy note: if the left gutter/indent strip ignores all hierarchy USS selectors, that strip is likely drawn by Unity's IMGUI/native TreeView layer and needs session GUIStyle bridge coverage rather than generated USS. */

/* Bridge disabled. No overrides emitted. */
