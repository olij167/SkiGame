using System.Collections;
using System.Collections.Generic;
using UnityEngine;
namespace Artngame.PDM.Kvant
{
    public class SprayInteractor : MonoBehaviour
    {

        public bool isExplosion = false; //constant or exploding
        public float interactPower = 1;
        public float interactExponent = 1;

        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}